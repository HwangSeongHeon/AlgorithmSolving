-- 코드를 작성해주세요




SELECT ID, FISH_NAME, LENGTH
FROM FISH_INFO
NATURAL JOIN FISH_NAME_INFO FNI
WHERE (FISH_TYPE, LENGTH)
IN (
SELECT FISH_TYPE, MAX(LENGTH) AS LENGTH FROM FISH_INFO FI
NATURAL JOIN FISH_NAME_INFO FNI
GROUP BY FISH_TYPE
)
ORDER BY ID;


